/**
 * Base URL for all backend API requests.
 *
 * @remarks
 * This value is used throughout the service layer to construct
 * absolute request paths. In production, consider replacing this
 * with an environment‑based configuration (e.g. Vite env vars).
 */
export const BASE_URL = "http://localhost:8000/api";
